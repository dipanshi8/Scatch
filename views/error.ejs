<%- include('./partials/header') %>

<div class="min-h-screen flex items-center justify-center bg-gray-100">
    <div class="text-center">
        <h1 class="text-6xl font-bold text-gray-800 mb-4"><%= statusCode || 500 %></h1>
        <h2 class="text-2xl font-semibold text-gray-600 mb-4">
            <%= error || 'Something went wrong' %>
        </h2>
        <p class="text-gray-500 mb-8">We're sorry, but something went wrong.</p>
        <a href="/" class="inline-block px-6 py-3 bg-blue-500 text-white rounded-lg hover:bg-blue-600 transition">
            Go Back Home
        </a>
    </div>
</div>

<%- include('./partials/footer') %>

